# Threads per service.
threads: 2
upstream_debug_ssl_keylog: true

proxy:
  http_bind_addrs:
  - 0.0.0.0:8080
  https_bind_addrs:
  - 0.0.0.0:4433

# api:
#   tls: true
#   cert: api.crt
#   key: api.key

cache:
  max_size: 100000000
